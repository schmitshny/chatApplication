import styled from 'styled-components';

export const IconsWrapper = styled.div`
  display: flex;
  gap: 10px;
  margin: 0 auto;
  width: 100%;
  position: absolute;
  bottom: 20px;
  left: 50%;
  z-index: 5;
`;
