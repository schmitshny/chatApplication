import styled from 'styled-components';

export const StyledStory = styled.div`
  border-radius: 50%;
  border: 2px solid;
  border-color: linear-gradient(to right, red, orange, yellow, green, blue, indigo, violet) 1;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  cursor: pointer;
`;
